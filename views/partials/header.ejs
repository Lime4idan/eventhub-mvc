<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= titulo %> | EventHub</title>
  <meta name="description" content="Plan events, welcome attendees, and keep every registration organized with EventHub.">
  <meta name="theme-color" content="#f7f4e8">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar navbar-roxa">
    <div class="container d-flex flex-wrap gap-3">
      <a class="navbar-brand" href="/" aria-label="EventHub home">
        <span class="brand-mark" aria-hidden="true">EH</span>
        <span class="brand-text">EventHub<small>events in motion</small></span>
      </a>
      <div class="d-flex align-items-center flex-wrap gap-3 ms-auto">
      <% if (usuario) { %>
        <a class="nav-link" href="/eventos">Events</a>
        <% if (usuario.tipo === 'participante') { %><a class="nav-link" href="/inscricoes/minhas">My registrations</a><% } %>
        <span class="nav-user">Hello, <%= usuario.nome %></span>
        <form action="/logout" method="post"><button class="btn btn-sm btn-outline-primary" type="submit">Sign out</button></form>
      <% } else { %>
        <a class="btn btn-sm btn-outline-primary" href="/login">Sign in</a><a class="btn btn-sm btn-primary" href="/cadastro">Register</a>
      <% } %>
      </div>
    </div>
  </nav>
  <main class="container py-5 pagina">
    <% if (mensagemSucesso) { %><div class="alert alert-success"><%= mensagemSucesso %></div><% } %>
    <% if (mensagemErro) { %><div class="alert alert-danger"><%= mensagemErro %></div><% } %>
